<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devdream.diverjoy">

    <application
        android:name=".context.AppContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.MenuActivity" />
        <activity android:name=".activities.GameStartActivity" />

        <activity android:name=".activities.inever.INeverPlayActivity" />
        <activity android:name=".activities.inever.INeverScoreActivity" />

        <activity android:name=".activities.mime.MimePlayActivity" />
        <activity android:name=".activities.mime.MimeScoreActivity" />

        <activity android:name=".activities.taboo.TabooPlayActivity" />
        <activity android:name=".activities.taboo.TabooScoreActivity" />
    </application>

</manifest>
